{% include 'default/_form.html.twig' with {
    page_title: 'title.advertisement_activate'|trans({'%id%': advertisement.id|default('')}),
    form: form,
    submit_label: 'action.activate'|trans,
    back_to_list_path: 'advertisement_index',
} %}